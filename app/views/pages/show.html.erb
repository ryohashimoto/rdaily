<% content_for :title do %>
  <%= rdaily_title(@page.title) %>
<% end %>
<div class="row">
  <div class="large-12 columns">
    <%= render :partial => 'page', :locals => { :page => @page } %>
  </div>
</div>