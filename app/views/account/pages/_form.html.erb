<%= simple_form_for [:account, page] do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.input :slug, placeholder: 'option' %>
  <%= f.button :submit, class: 'small success button' %>
<% end %>
