language: ruby
rvm:
  - 1.9.3
  - 2.0.0
gemfile:
  - Gemfile
env:
  - DB=sqlite3
script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate
  - bundle exec rake db:test:prepare
  - bundle exec rspec spec/
notifications:
  emails:
    - ryohashimoto@gmail.com
  on_success: always
  on_failure: always
